#include "main.h"

/**
 * _strlen - gives length of a string
 * @s: represents the  array
 * Return: no return
 */
int _strlen(char *s)
{
	int l;
	
	for (l = 0; s[i] != '\0'; i++)
	{
		;
	}
	return (i);
}
