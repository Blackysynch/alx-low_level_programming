#include "main.h"
#include <stdio.h>

/**
 * swap_int - swap two different integers
 * @a: integer to be swaped
 * @b: integer to be swaped
 * Return: Always value
 */
void swap_int(int *a, int *b)
{
	*b = 98;
	*a = 42;
}
