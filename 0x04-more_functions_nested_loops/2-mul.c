#include <stdio.h>
#include "main.h"
/**
 * mul - perform multiplication
 * @a: integer variable
 * @b: integer variable
 * Return: 0 always
 */

int mul(int a, int b)
{
	int a;
	int b;

	putchar(a * b);
	return (0);
}
