#include "main.h"
#include <stdio.h>

/**
 * _strlen - gives string length
 * @s: string
 * Return: no return value
 */
int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0'; l++)
	{
		;
	}
	return (l);
}
